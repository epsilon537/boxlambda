// DESCRIPTION: Verilator output: Waivers generated with --waiver-output

`verilator_config

// Below you find suggested waivers. You have three options:
//   1. Fix the reason for the linter warning
//   2. Keep the waiver permanently if you are sure this is okay
//   3. Keep the waiver temporarily to suppress the output

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p0_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p1_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p2_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 10 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_dfi_p3_address' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_new_banks_read7' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_new_banks_read7' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_new_banks_read7' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_new_banks_read7' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel0_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel1_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel2_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel3_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel4_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel5_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel6_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 21 bits on the Assign RHS, but Assign RHS's SEL generates 22 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's VARREF 'soc_ddrphy_bankmodel7_write' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Logical operator IF expects 1 bit on the If, but If's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 10 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 27 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine0_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine1_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine2_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNW expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine3_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine4_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine5_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine6_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SHIFTL expects 14 bits on the LHS, but LHS's VARREF 'soc_litedramcore_bankmachine7_auto_precharge' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 14 bits on the RHS, but RHS's REPLICATE generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 4 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ADD expects 3 bits on the LHS, but LHS's SEL generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ADD expects 3 bits on the RHS, but RHS's SEL generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ADD expects 3 bits on the RHS, but RHS's SEL generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ADD expects 3 bits on the RHS, but RHS's SEL generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ADD expects 3 bits on the RHS, but RHS's SEL generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 4 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 4 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 128 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 16 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SUB expects 24 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator SUB expects 24 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 2 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's VARREF 'litedramcore_wishbone_adr' generates 30 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 4 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 5 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'h9' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'h9' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'ha' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'ha' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hb' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hb' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hc' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hc' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hd' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hd' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'he' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'he' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hf' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '4'hf' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h10' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h10' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h11' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h11' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h12' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h12' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h13' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h13' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h14' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h14' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h15' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h15' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h16' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h16' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h17' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h17' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h18' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 9 bits on the RHS, but RHS's CONST '5'h18' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel0_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel0_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel1_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel1_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel2_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel2_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel3_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel3_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel4_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel4_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel5_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel5_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel6_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel6_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel7_write_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator OR expects 25 bits on the RHS, but RHS's VARREF 'soc_ddrphy_bankmodel7_read_col' generates 10 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 2 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '11'h400' generates 11 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 6 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '11'h400' generates 11 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 6 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 6 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 6 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 6 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '11'h400' generates 11 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 5 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 5 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 5 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator NEQ expects 5 bits on the RHS, but RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 3 bits on the Assign RHS, but Assign RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 3 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 5 bits on the Assign RHS, but Assign RHS's REPLICATE generates 6 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator EQ expects 3 bits on the RHS, but RHS's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank0_init_done0_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank0_init_error0_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_control0_w' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi0_command0_w' generates 6 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'soc_litedramcore_phaseinjector0_command_issue_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi0_address0_w' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi0_baddress0_w' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi1_command0_w' generates 6 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'soc_litedramcore_phaseinjector1_command_issue_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi1_address0_w' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi1_baddress0_w' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi2_command0_w' generates 6 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'soc_litedramcore_phaseinjector2_command_issue_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi2_address0_w' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi2_baddress0_w' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi3_command0_w' generates 6 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'soc_litedramcore_phaseinjector3_command_issue_w' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi3_address0_w' generates 14 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csrbank1_dfii_pi3_baddress0_w' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '1'h0' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '1'h1' generates 1 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '2'h2' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '2'h3' generates 2 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '3'h4' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '3'h5' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '3'h6' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '3'h7' generates 3 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'h8' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'h9' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'ha' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'hb' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'hc' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'hd' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'he' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '4'hf' generates 4 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h10' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h11' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h12' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h13' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h14' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h15' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h16' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h17' generates 5 bits."

lint_off -rule WIDTH -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Operator CASE expects 9 bits on the Case Item, but Case Item's CONST '5'h18' generates 5 bits."

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Bits of signal are not used: 'wb_ctrl_adr'[31:30]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Bits of signal are not used: 'user_port_wishbone_p_0_adr'[31:24]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Bits of signal are not used: 'user_port_wishbone_p_1_adr'[31:24]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Bits of signal are not used: 'user_port_wishbone_c_0_dat_r'[127:32]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Bits of signal are not used: 'user_port_wishbone_c_1_dat_r'[127:32]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'sim_trace'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_cke'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_odt'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_reset_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_act_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_wrdata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p0_rddata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_cke'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_odt'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_reset_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_act_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_wrdata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p1_rddata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_cke'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_odt'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_reset_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_act_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_wrdata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p2_rddata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_cke'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_odt'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_reset_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_act_n'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_wrdata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_dfi_p3_rddata_en'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel0_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel1_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel2_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel3_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel4_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel5_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel6_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_ddrphy_bankmodel7_write_port_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p0_rddata'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p0_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p1_rddata'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p1_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p2_rddata'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p2_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p3_rddata'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_ext_dfi_p3_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_command_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_command_issue_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_command_issue_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_address_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_baddress_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_wrdata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_rddata_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector0_rddata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_command_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_command_issue_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_command_issue_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_address_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_baddress_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_wrdata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_rddata_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector1_rddata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_command_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_command_issue_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_command_issue_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_address_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_baddress_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_wrdata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_rddata_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector2_rddata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_command_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_command_issue_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_command_issue_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_address_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_baddress_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_wrdata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_rddata_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_phaseinjector3_rddata_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_dfi_p0_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_dfi_p1_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_dfi_p2_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_dfi_p3_rddata_valid'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_timer_count0'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_zqcs_timer_count0'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine0_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine0_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine0_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine1_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine1_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine1_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine2_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine2_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine2_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine3_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine3_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine3_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine4_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine4_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine4_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine5_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine5_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine5_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine6_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine6_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine6_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine7_wrport_dat_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine7_source_source_first'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_bankmachine7_source_source_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_choose_cmd_cmd_payload_is_cmd'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramcore_choose_req_cmd_payload_is_cmd'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_init_done_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_init_error_re'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport0_flush'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport0_cmd_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport0_rdata_ready'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport1_flush'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport1_cmd_last'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'soc_litedramnativeport1_rdata_ready'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'litedramcore_wishbone_adr'[29:14]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'litedramcore_wishbone_cti'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'litedramcore_wishbone_bte'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'interface0_bank_bus_dat_w'[31:1]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank0_init_done0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank0_init_error0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_control0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi0_command0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi0_address0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi0_baddress0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi0_wrdata0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi0_rddata_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi1_command0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi1_address0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi1_baddress0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi1_wrdata0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi1_rddata_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi2_command0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi2_address0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi2_baddress0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi2_wrdata0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi2_rddata_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi3_command0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi3_address0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi3_baddress0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi3_wrdata0_we'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Signal is not used: 'csrbank1_dfii_pi3_rddata_r'"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy0'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy1'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy2'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy3'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy4'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy5'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy6'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy7'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy8'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy9'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy10'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy11'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy12'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy13'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy14'[2:0]"

lint_off -rule UNUSED -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Bits of signal are not used: 'slice_proxy15'[2:0]"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x0)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x2)"

lint_off -rule CASEINCOMPLETE -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Case values incompletely covered (example pattern 0x19)"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule COMBDLY -file "*/mnt/c/work/boxlambda/components/litedram/sim/rtl/litedram.v" -match "Delayed assignments (<=) in non-clocked (non flop or latch) block*"

lint_off -rule IMPERFECTSCH -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Imperfect scheduling of variable: 'litedram_wrapper.user_port_wishbone_c_0_ack'"

lint_off -rule IMPERFECTSCH -file "*/mnt/c/work/boxlambda/components/litedram/common/rtl/litedram_wrapper.sv" -match "Imperfect scheduling of variable: 'litedram_wrapper.user_port_wishbone_c_1_ack'"
